<UserControl x:Class="hotel_management_front.tabsUserControl.etatLieuChambreUserControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:hotel_management_front.tabsUserControl"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*"/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <!--left grid--> 
        <UniformGrid  x:Name="mainGrid" Margin="30" Grid.Column="0">
            
        </UniformGrid>
        
        <!--right grid-->
        <Grid Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition/>
                <RowDefinition/>
            </Grid.RowDefinitions>
            <StackPanel Orientation="Vertical" x:Name="topGrid" Grid.Row="0"></StackPanel>
            <Grid x:Name="bottomGrid" Grid.Row="1"></Grid>
        </Grid>
    </Grid>
</UserControl>
